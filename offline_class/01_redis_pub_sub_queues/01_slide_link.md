## redis, pub sub and queues class link is here:

```link
https://projects.100xdevs.com/tracks/Redis/Redis1
```

## Commands:

```sh
docker run --name redis -d -p 6379:6379 redis

```


# restart from 1:02:20

Q. why does redis have its own cli, like example postgres has its own psql cli for managing data visually, but why does the redis?